import ConfirmacionClient from "./ConfirmacionClient";

export const metadata = {
  title: "Pedido confirmado — Frescon",
};

export default function ConfirmacionPage() {
  return <ConfirmacionClient />;
}
